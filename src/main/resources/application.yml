spring:
  datasource:
    url: jdbc:mysql://localhost:3306/projectlab?createDatabaseIfNotExist=true&useSSL=false
    username: oleh
    password: lenovog50
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: create
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 8080

